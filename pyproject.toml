[project]
name = "curador-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aliwen-Yafu-Kauel", email = "aliwen.yafu@kauel.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "opencv-python>=5.0.0.93",
    "pyqt6>=6.11.0",
]

[project.scripts]
curador-data = "curador_data:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
